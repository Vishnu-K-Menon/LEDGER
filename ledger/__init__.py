"""LEDGER v1 — Loop with Evidence-Decomposed Grounding and Error Repair.

Paths in CLAUDE.md (``tracing/otel.py``, ``eval/kappa.py``, ...) are relative to this package.
The claim-ledger data structure lives at ``ledger/claims/ledger.py`` — never ``ledger/ledger.py``.
"""

__version__ = "0.1.0"
